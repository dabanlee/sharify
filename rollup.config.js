export const paths = {
    root: '/',
    source: {
        root: './src/',
        modules: './src/modules/',
    },
    dist: {
        root: './dist/',
    },
};

export default {
	moduleName: 'Sharify', // var Sharify = (function () { ...
	moduleId: 'Sharify', // define(['Sharify'], ...
	fileName: 'sharify',
}
