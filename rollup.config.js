export const paths = {
    root: '/',
    source: {
        root: './src/',
        modules: './src/modules/',
    },
    dist: {
        root: './dist/',
    },
};

export default {
	moduleName: 'sharify', // var sharify = (function () { ...
	moduleId: 'sharify', // define(['sharify'], ...
}
